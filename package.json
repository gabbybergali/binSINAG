{
  "name": "binsinag-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd lgu-dashboard && npm install && npm run build",
    "dev": "cd lgu-dashboard && npm run dev"
  }
}
